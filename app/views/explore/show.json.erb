<%=

{
  :id => @mapping.id,
  :title => format_title(@mapping.title),
  :old_url => @mapping.old_url,
  :new_url => @mapping.new_url,
  :status => @mapping.status,
  :result => @review.present? ? @review.result : nil
}.to_json.html_safe

%>
